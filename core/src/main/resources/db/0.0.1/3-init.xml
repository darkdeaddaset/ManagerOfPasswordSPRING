<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.1.xsd">
    <changeSet author="kmve_afw" id="3">
        <sql>
            insert into departments(id, name, password) values (999999, 'default', 'default');

            insert into users(id, login, password) values (999999, 'test', 'test');

--             insert into users_departments(user_id, department_id) values (100, 999999);

            insert into admin(user_id, department_id) values (999999, 999999);

            insert into users_roles(user_id, roles) values (999999, 0);
        </sql>
    </changeSet>
</databaseChangeLog>